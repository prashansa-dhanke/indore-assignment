import  Colors  from "../components/Colors";

const THEME = {
    safeAreaView: {
        flex:1,
        backgroundColor: Colors.white
    },
    defaultBackround:{
        flex:1,
        backgroundColor: Colors.white
    }
}

export default THEME;